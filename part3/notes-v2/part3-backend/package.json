{
  "name": "part3-backend",
  "version": "1.0.0",
  "description": "Hello World Project",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../part3-notes && npm run build --prod && cp -r build ../part3-backend/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "author": "Andrew Plotner",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
